# ─── MineAssist 994F – Frontend ───────────────────────────────────────────────
# Copier ce fichier en `.env.local` à la racine de `frontend/` et compléter les
# valeurs.  Vite injecte automatiquement les variables préfixées par `VITE_`.

# URL de base de l'API FastAPI
VITE_API_URL=http://127.0.0.1:8000
